Update `src/components/CapabilitiesSection.tsx` so the Calculator and Cog icons render white instead of muted gray.

## Change
- Replace `text-muted-foreground` with `text-slate-50` on both icons (lines 6 and 11), matching the white styling used elsewhere in the recent edits.
- Leave the third card (American flag image) untouched.

## Result
Both the "Algorithmic Quoting" and "Precision Machining" icons appear white, consistent with the navy/white palette.